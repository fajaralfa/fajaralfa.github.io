<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%- page.title ?? config.title %></title>
    <%- css('css/style.css') %>
</head>

<body class="bg-white dark:bg-gray-900 dark:text-slate-200">
    <div class="max-w-[50rem] mx-auto mt-5 px-3">
        <nav class="px-4 flex justify-between gap-x-5 text-xl mb-5">
            <span>
                <% if (path != 'index.html') { %>
                    <a href="<%= url_for('/') %>" class="hover:underline">Posts</a>
                <% } %>
            </span>
            <span><a href="<%= url_for('about') %>" class="hover:underline">About</a></span>
        </nav>

        <div>
            <%- body %>
        </div>

        <footer class="mt-8 text-center">
            fajaralfa
        </footer>
    </div>

    <button class="fixed right-0 bottom-0 px-2" id="theme-switcher">
        <img src="/icon/theme-switcher.svg" alt="" srcset="" class="w-10 border-none">
    </button>
    <script>
        const themeSwitcher = document.getElementById('theme-switcher')
        themeSwitcher.addEventListener('click', () => {
            const htmlClassList = document.querySelector('html').classList
            if (htmlClassList.contains('dark')) htmlClassList.remove('dark')
            else htmlClassList.add('dark')
        })
    </script>
</body>

</html>